The changes only include adding a .gitignore file, modifying README.md and TZ.md files. These are all source/config files and there are no build artifacts, dependencies, or temporary files in the change list.

(empty)