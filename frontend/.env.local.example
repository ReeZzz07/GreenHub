NEXT_PUBLIC_API_URL=http://localhost:4000/api

# Хост картинок в проде (Yandex Object Storage) для next/image remotePatterns.
# Локально не нужен — localhost:9000 (MinIO) уже разрешён по умолчанию.
# NEXT_PUBLIC_IMAGE_HOSTNAME=my-bucket.storage.yandexcloud.net
