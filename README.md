# GreenHub

PWA-маркетплейс для C2C и B2C торговли растениями, саженцами, семенами и сопутствующими товарами.

## 📖 О проекте

GreenHub — это прогрессивное веб-приложение (PWA), которое соединяет продавцов и покупателей растений в России. Платформа выступает информационным посредником, предоставляя инструменты для размещения объявлений, коммуникации и безопасных платежей через внешние платежные системы.

**Ключевые особенности:**
- 🌱 AI-распознавание растений по фото
- ✨ Автоматическая генерация описаний с помощью LLM
- 🔒 Вотермарки на всех изображениях для защиты контента
- 💬 Встроенный чат между покупателями и продавцами
- 📱 PWA с оффлайн-режимом для просмотра каталога
- 🛡️ Модерация объявлений и фитосанитарный контроль

## 📋 Документация

- [Техническое задание (TZ.md)](./TZ.md) — полное описание требований, архитектуры и плана разработки

## 🚧 Статус проекта

- ✅ **Frontend** (`frontend/`) — Next.js-приложение с роутами `/`, `/catalog`, `/recognize`, `/cart`, `/profile`, `/plant/[id]`, UI-компоненты, контексты корзины/авторизации; пока на моковых данных, не подключён к API
- ✅ **Backend** (`backend/`) — стартовый каркас NestJS: авторизация по JWT (`/auth/register`, `/auth/login`, `/auth/me`), CRUD пользователей и категорий, Prisma-схема под PostgreSQL с ролевой моделью
- ❌ Объявления/каталог, чат, очереди (BullMQ), загрузка медиа в S3, вотермарки — не реализованы
- ❌ Интеграции с Plant.id, LLM, ЮKassa/Robokassa — не подключены
- ❌ Frontend и backend пока не связаны между собой

Разделы ниже описывают **целевую архитектуру MVP** согласно [TZ.md](./TZ.md), а не всё, что уже работает.

## 🛠 Технологический стек

### Frontend
- **Next.js** + TypeScript — SSR/SSG рендеринг
- **Tailwind CSS** — стилизация
- **PWA** — Service Workers для оффлайн-работы

### Backend
- **NestJS** + TypeScript — модульная архитектура API
- **Prisma/TypeORM** — ORM для работы с БД
- **BullMQ + Redis** — очереди задач для фоновой обработки
- **Sharp** — обработка изображений (сжатие, вотермарки)

### База данных и кэширование
- **PostgreSQL** — основное хранилище данных
- **Redis** — кэширование, сессии, очереди

### Инфраструктура
- **Docker + Docker Compose** — контейнеризация
- **Yandex Object Storage (S3)** — хранение медиафайлов
- **VPS (Ubuntu)** — хостинг backend и БД
- **Vercel / Nginx** — хостинг frontend
- **Prometheus + Grafana** — мониторинг
- **Winston** — логирование

### Внешние сервисы
- **Plant.id API** — распознавание растений
- **Qwen API (или аналоги)** — генерация описаний
- **ЮKassa / Robokassa** — платежные ссылки

## 🚀 Быстрый старт

### Предварительные требования
- Node.js 18+
- Docker и Docker Compose (для PostgreSQL/Redis, нужны только бэкенду)

### Frontend

```bash
git clone https://github.com/ReeZzz07/GreenHub.git
cd GreenHub/frontend
npm install
npm run dev
```

Приложение будет доступно на [http://localhost:3000](http://localhost:3000).

Другие команды: `npm run build` — production-сборка, `npm run start` — запуск сборки, `npm run lint` — линтер.

### Backend

```bash
# из корня репозитория — поднимает PostgreSQL и Redis
docker-compose up -d

cd backend
npm install
cp .env.example .env

npx prisma migrate dev   # создаёт таблицы по schema.prisma
npm run start:dev
```

API будет доступно на [http://localhost:4000/api](http://localhost:4000/api) (например, `GET /api/health`).

Другие команды: `npm run build` — компиляция в `dist/`, `npm run prisma:studio` — GUI для просмотра БД.

## 📁 Структура проекта

```
GreenHub/
├── TZ.md                       # Техническое задание
├── README.md
├── docker-compose.yml           # PostgreSQL + Redis для локальной разработки
├── frontend/                    # Next.js приложение (App Router)
│   ├── public/
│   │   └── manifest.json        # PWA-манифест
│   └── src/
│       ├── app/                  # Роуты: /, /catalog, /recognize, /cart, /profile, /plant/[id]
│       ├── components/           # Переиспользуемые UI-компоненты
│       ├── context/              # AuthContext, CartContext (React Context + localStorage)
│       ├── data/                 # Моковые данные (mockPlants.ts)
│       └── types/                # TypeScript-типы и enum'ы
└── backend/                      # NestJS API
    ├── prisma/
    │   └── schema.prisma          # Модели User, Category + роли
    └── src/
        ├── auth/                  # JWT-регистрация/логин, guards, роли
        ├── users/                 # CRUD пользователей
        ├── categories/            # CRUD категорий (админ)
        └── prisma/                # PrismaService (подключение к БД)
```

Ещё не реализовано согласно [TZ.md](./TZ.md): объявления/каталог, чат, загрузка медиа в S3 с вотермарками, очереди BullMQ, интеграции Plant.id/LLM/ЮKassa, мониторинг и бэкапы.

## 🔑 Основные возможности MVP

### Для покупателей
- Просмотр каталога растений с фильтрами
- Поиск по категориям, тегам и атрибутам
- Добавление товаров в избранное
- Чат с продавцами
- Оплата через защищенные платежные ссылки

### Для продавцов
- Создание объявлений с AI-помощником
- Автоматическое распознавание растений по фото
- Генерация продающих описаний через LLM
- Управление объявлениями и статусами
- Базовая аналитика (просмотры, избранное, клики)
- Подписка для питомников (массовое управление)

### Для модераторов
- Проверка новых объявлений (SLA 24 часа)
- Верификация фитосанитарных сертификатов
- Модерация чатов и контента
- Шаблоны быстрых ответов для отклонения

### Для администраторов
- CRUD категорий и подкатегорий
- Управление ролями пользователей
- Настройка тарифов подписок
- Полный доступ ко всем данным платформы

## 📊 Метрики качества

- **Lighthouse Score:** ≥ 90 (Performance, Best Practices, SEO)
- **Время модерации:** ≤ 24 часа в рабочие дни
- **Доступность:** WCAG 2.1 AA
- **Бэкапы БД:** ежедневно, хранение 7 дней
- **AI лимиты:** 5 запросов в час на пользователя

## 🔐 Безопасность

- HTTPS для всех соединений
- JWT авторизация
- Защита от OWASP Top 10 уязвимостей
- Одноразовые платежные ссылки (24 часа)
- Премодерация AI-генерируемого контента
- Вотермарки на всех изображениях

## 📈 План развития

| Этап | Длительность | Фокус |
|------|--------------|-------|
| 1 | Недели 1-3 | Фундамент, Бэкенд, Инфраструктура |
| 2 | Недели 4-5 | Каталог, AI-интеграции |
| 3 | Недели 6-8 | Frontend, PWA, UI/UX |
| 4 | Недели 9-10 | Чат, Платежи, Модерация |
| 5 | Недели 11-12 | Тестирование, Релиз |

## 🤝 Вклад в проект

Если вы хотите внести свой вклад, пожалуйста:
1. Создайте fork репозитория
2. Создайте ветку для вашей фичи (`git checkout -b feature/amazing-feature`)
3. Закоммитьте изменения (`git commit -m 'Add amazing feature'`)
4. Отправьте в удаленный репозиторий (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## 📄 Лицензия

[Укажите лицензию, например: MIT]

## 📞 Контакты

- **Автор:** Резик
- **Email:** [your-email@example.com]
- **Документация:** См. [TZ.md](./TZ.md)

---

**Версия документа:** 1.1  
**Последнее обновление:** 25 августа 2026
